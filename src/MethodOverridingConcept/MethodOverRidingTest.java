package MethodOverridingConcept;

public class MethodOverRidingTest {
	
	//Method overriding we can perform once inheritence is done then only we can perform
	
	//Alreday method implemented in parent calss same method we are going to implement in chaild calss with same parameters and same return type
	
	//Method overriding is the run time polymarhpisam
	
	//Method name must be same
	//same parameters list is required
	//Return must be same
	
	//Can we override the static methods-No
	//Can't able to override final methods

}
